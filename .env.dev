#API
PORT=3001
NODE_ENV=development
SERVICE_NAME=ApiTSExpress
API_VERSION=1.2.0
# Auth
JWT_SECRET=change_me_super_secret
#DataSource: "dummy" (in-memory, default) | "sqlserver" | "oracle"
DATA_SOURCE=oracle
#DB (SQL Server)
DB_DIALECT=mssql
DB_HOST=localhost
DB_PORT=1434
DB_USER=sa
DB_PASSWORD=StrongPassword123!
DB_NAME=testdb
#Oracle (docker compose up oracle). Required only when DATA_SOURCE=oracle.
ORACLE_USER=appuser
ORACLE_PASSWORD=AppPassword1
ORACLE_CONNECT_STRING=localhost:1521/FREEPDB1
ORACLE_POOL_MIN=1
ORACLE_POOL_MAX=10
ORACLE_POOL_INCREMENT=1
#SYS password, only used by docker-compose to bootstrap the container.
ORACLE_SYS_PASSWORD=OraclePassword1
ORACLE_PORT=1521
#LOG
#Logger implementation: "pino" (default) | "winston"
LOG_DRIVER=pino
LOG_LEVEL=trace
